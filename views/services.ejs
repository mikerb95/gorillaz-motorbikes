<%- include('partials/head', { title: 'Servicios — Gorillaz Motorbikes', user }) %>

<section class="container">
  <h1>Servicios</h1>
  <p>Ofrecemos atención integral para tu moto:</p>
  <ul class="grid">
    <% services.forEach(s => { %>
      <li><%= s %></li>
    <% }) %>
  </ul>
</section>

<%- include('partials/footer') %>
